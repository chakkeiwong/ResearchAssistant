# M23 Versioned Clean-Checkout Close Subplan

Date: `2026-07-19`
Status: `PASSED_CLEAN_COMMIT_AND_TERMINAL_REVIEW`

## Phase Objective

Convert the passing dirty-worktree M23 candidate into a versioned integration
candidate that a clean checkout can rebuild, test, and accept without hidden
local validation roots or repository build scratch.

## Entry Conditions

- `_r7` passed all nine M23 operational cases and installed replay.
- Focused M23 tests passed `11/11`; the affected gate passed
  `262 passed, 77 deselected`.
- Fallback Review Round 3 returned `REVISE` only because the complete supporting
  state is not versioned and clean-checkout reproducibility is unsupported.
- No network, credential, PDF fallback, release, push, or public action is in
  scope.

## Required Artifacts

1. Repaired M23 runner that generates a fresh M22 replay root inside every M23
   acceptance root.
2. Exact Git include manifest covering:
   - changed and new runtime source, scripts, tests, and operator docs;
   - active M17-M23 master/subplans/results and terminal review records;
   - compact M20/M21/M22 authorities named by the active M22 matrix;
   - the active M22 matrix, qualitative bundle, triage/inspection records,
     selected queue authority, and exact cited TeX source members.
3. Exact exclude manifest covering raw archive bodies, complete downloaded
   source campaigns beyond cited members, wheels, venvs, command transcripts,
   copied execution sources, caches, logs, and unrelated dirty files.
4. One integration commit candidate and one clean temporary checkout.
5. Fresh clean-checkout M23 validation root, result refresh, and terminal review.

## Required Checks And Reviews

- Skeptical audit of the include/exclude closure for missing runtime, test,
  document, and evidence dependencies.
- Focused M22/M23 tests before Git mutation.
- `git diff --check`, JSON parsing, and no unexpected nonignored files.
- From the exact clean checkout: compile, focused M23 tests, the exact affected
  M16/M17/M20/M22/M23/CLI gate, fresh M23 acceptance, installed replay from
  `/tmp`, source/wheel module equality, empty stderr, and artifact parsing.
- One fresh terminal read-only review of the clean-checkout candidate.

## Evidence Contract

The clean-checkout commit, tree, exact staged path list, clean status, wheel
hash, source/wheel module hash, command ledger, generated M22 replay, acceptance
root, test commands/results, and review verdict must be recorded. Scientific
source descriptions remain bound to the exact cited local TeX members. The
commit does not claim literature completeness, claim truth, publication
safety, human usability, product readiness, or release readiness.

## Forbidden Claims And Actions

- Do not restore the accomplished label before the clean-checkout acceptance
  and terminal review pass.
- Do not commit raw accepted-source archive bodies, venvs, wheels, logs,
  command transcripts, or unrelated generated roots merely to make replay pass.
- Do not weaken source-anchor validation, M22 limitation reporting, tamper
  rejection, or the affected test gate.
- Do not push, release, access credentials, dispatch providers, fetch sources,
  or cross a publication/privacy boundary.

## Exact Handoff Conditions

The master program may close only when an identified clean commit contains the
complete declared dependency closure; a clean checkout has no hidden runtime or
evidence dependency; all required checks pass; fresh M23 acceptance generates
and replays M22 inside its own root; and terminal review returns `AGREE`.

After agreement, write the final close record, change master/reset to
`ACCOMPLISHED_WITHIN_RECORDED_LOCAL_EXPLORATORY_SCOPE`, and record the exact
commit/evidence/review identity. Push and release remain separately prohibited.

## Stop Conditions

Stop for missing Git authorization, overlapping user work that cannot be
preserved, a required raw/private source that cannot be versioned or compactly
represented, a clean-checkout test or replay defect that cannot be repaired
within scope, or terminal review nonconvergence at five material rounds.

## Skeptical Audit

The dirty-worktree wheel is not a clean-checkout comparator. Test success alone
does not prove that ignored evidence is reconstructible. The earliest
discriminating repair is to generate M22 within M23, version only the compact
authority/source closure, and run the exact gate from a clean checkout. This
answers the Round 3 finding without converting Git ceremony into scientific
evidence or committing bulk downloaded artifacts.

Audit verdict: `PASS_TO_GIT_AUTHORIZATION_BOUNDARY`.

## Pre-Authorization Candidate Diagnostic

The exact include manifest contains `101` files and has SHA-256
`d5e3965530a20e1fb4993e61a2bda920faf4ef2761c23794f3604f8129f0fb1f`.
The exclude manifest has SHA-256
`9d31aaa615c9f83deb91d8c65cfc60d9c3ecc4b2db93aa63aaa04e4a7ba28157`.

The prior `99`-path diagnostic candidate omitted the modified retained-source
provenance catching test and is superseded. No real-repository staging or
commit occurred. Codex must rebuild a temporary local Git repository from
`HEAD` plus exactly the corrected manifest before requesting Git authorization.

The first corrected staging audit also found that the verbatim upstream TeX
snapshots contain original trailing whitespace. Normalizing those files would
change the preserved evidence bytes. The closure therefore includes a narrow
`.gitattributes` rule that disables Git whitespace diagnostics only under the
two declared academic source-snapshot trees; code, tests, plans, JSON, and
operator documentation remain under the full whitespace gate.

The superseded candidate had passed:

- seven focused missing-dependency/retained-reconciliation catches;
- twenty focused M22/M23 tests;
- the exact affected gate: `263 passed, 77 deselected`;
- fresh M23 acceptance: `9/9`, `M23_OPERATIONAL_ACCEPTANCE_PASSED`;
- generated M22 replay inside the M23 root with forward citations unavailable,
  50 identifier-bearing rows open, and 195 identifier-free units open;
- installed replay from `/tmp` with `PYTHONPATH` unset;
- empty command stderr files; and
- source/wheel M23 module equality at SHA-256
  `14173a0f5dedfa9c7e087dfa1f880ad245fab7038ab6dd15ed6b75176903bc6b`.

The temporary M23 wheel SHA-256 was
`ed1c2593052cd16f4ff807ff9447401646d07f99f21c87490892c8d4ba932341`.
These diagnostics establish that the proposed boundary is feasible; they do not
authorize or substitute for the real integration commit and clean checkout.

## Phase Result

The real local integration commit is
`6149818ab25791ca01c9d84fbbbb580f1e121841`, tree
`60467239d7ccfd5f035049f6ca6913a880d3ba23`. Its exact changed path set matches
the 101-path include manifest and contains none of the exclude-manifest bulk
artifacts.

A detached clean checkout passed:

- compile and `git diff --check`;
- `23` focused M22/M23 tests;
- the exact affected gate: `263 passed, 77 deselected`;
- fresh M23 acceptance: `9/9`, `M23_OPERATIONAL_ACCEPTANCE_PASSED`;
- installed replay from `/tmp` with `PYTHONPATH` unset;
- empty stderr and `75` parseable JSON artifacts; and
- source/wheel M23 module equality at SHA-256
  `14173a0f5dedfa9c7e087dfa1f880ad245fab7038ab6dd15ed6b75176903bc6b`.

The authoritative wheel SHA-256 is
`fdd58293702371e4f85efe1f667e5310e597073d35ca3a945b6b831d1cbe7899`.
Fresh Codex fallback Review Round 4 returned `AGREE`. Claude export remained
unavailable under the environment's external-data policy and was not routed
around.

Handoff: write and commit the ordinary close record, preserve all scientific
limitations and nonclaims, and do not push or release.
