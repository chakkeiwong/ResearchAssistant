# Literature Survey North-Star Program Setup Result

Date: `2026-07-13`
Status: `PROGRAM_SETUP_PASSED_M17_READY_NOT_STARTED`

## Decision

The converged north-star roadmap has been expanded into a visible gated program
candidate, and all declared local consistency/integrity checks pass. This setup
does not execute M17 product work. The user-authorized extra governance repair
replaced the circular M23 closeout with a fail-closed two-stage transaction, and
the bounded fresh read-only review returned `AGREE`. M17 is ready but not
started.

## Claimed Target And Quantity Produced

| Field | Value |
| --- | --- |
| Claimed target | A coherent M17-M23 master-program execution surface whose repair loop continues through expected failures and stops only at declared continuation/human boundaries. |
| Quantity produced | One visible runbook, execution ledger, stop/handoff record, contract-complete M17 subplan, six gated future subplans, master artifact index, and this setup result. |
| Relationship | Passing implementation of the planning target; it authorizes only the reviewed local M17 lane. |
| Not checked yet | M17 implementation and every later Git/live/source/human/acceptance capability. |

## Artifacts Created

- `docs/plans/literature_survey_north_star_visible_gated_execution_runbook_2026-07-13.md`
- `docs/plans/literature_survey_north_star_visible_execution_ledger_2026-07-13.md`
- `docs/plans/literature_survey_north_star_visible_stop_handoff_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m17_idea_topic_bootstrap_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m18_reproducible_git_integration_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m19_bounded_live_metadata_validation_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m20_live_discovery_and_citation_frontier_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m21_live_source_status_and_anchor_intake_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m22_human_attested_review_and_real_missions_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_m23_acceptance_and_operational_closeout_subplan_2026-07-13.md`
- `docs/plans/literature_survey_north_star_program_setup_review_nonconvergence_blocker_2026-07-13.md`
- final round-5 M17 and governance review verdicts under `docs/reviews`;
- this result.

The master program was updated with the exact setup index. The canonical
milestone JSON retains exactly M17 as active; program setup does not promote a
milestone.

## Skeptical Audit Result

| Risk | Setup treatment |
| --- | --- |
| Wrong baseline | M16 dirty-tree local authority is explicit; M18 owns Git reproducibility. |
| Proxy promotion | Fixture/review/file/count/timing evidence cannot close live, human, scientific, product, or north-star predicates. |
| Missing stop/repair logic | Every subplan distinguishes fixable/expected outcomes from continuation vetoes and caps one material review blocker at five rounds. |
| Unfair comparisons | Future provider/mission observations are descriptive unless a refreshed plan predeclares valid statistical evidence; no setup ranking occurs. |
| Hidden assumptions | Git, network, source rights/privacy, genuine human decisions, defaults, and release have separate approval gates. |
| Stale context | Every future shell requires refresh from the actual immediately preceding result. |
| Environment mismatch | M17 and local checks are CPU-only with GPU intentionally hidden; future runtime environments must be frozen during refresh. |
| Unfit commands/artifacts | M17 has exact local artifacts/checks; future unknown commands/hashes/routes are forbidden until refresh. |

The audit permits planning validation, not M17 product execution.

## Local Planning And Integrity Checks

`PASSED_AND_REPLAYED_AFTER_EXTRA_GOVERNANCE_REPAIR`

Passed checks:

- canonical milestone JSON parses with `CUDA_VISIBLE_DEVICES=-1`;
- exactly one active milestone, M17;
- all indexed setup artifacts exist and every declared future result is absent;
- each subplan contains every required contract section;
- exactly M17 is conditionally executable and M18-M23 are
  `REFRESH_AND_REVIEW_REQUIRED_DO_NOT_EXECUTE`;
- master/runbook/subplan phase names and paths agree;
- frozen M16 authority hashes remain exact;
- no stale status claims program or M17 completion;
- `git diff --check` passes for tracked changes and explicit new-file hygiene
  passes;
- trailing-whitespace scan passes.

## Review Contract And Provenance

The complete setup surface requires a fresh read-only review for:

- consistency with the converged master and milestone JSON;
- M17 identity, confirmation, bootstrap authority, crash/replay, compatibility,
  allowlist, evidence, handoff, and stop feasibility;
- M18-M23 inherited gates, artifact coverage, authority separation, expected
  failure continuation, and boundary safety;
- runbook repair-loop persistence and reviewer role limits; and
- unsupported claims, stale assumptions, missing tests/artifacts, or accidental
  future-phase execution authority.

Prior environment policy rejected Claude repository export. The setup must not
route around that rejection. A fresh Codex read-only reviewer is the approved
fallback, with downgraded provenance recorded. Review agreement remains
advisory and cannot override local checks or authorize any later boundary.

Review status: `M17_AGREE_EXTRA_GOVERNANCE_AGREE`.

Round-1 candidate was withdrawn before agreement after the supervisor found
that the Phase 10 `1,137`-row inventory is validation-tree-scoped and cannot by
itself bind product-file bytes. M17 now resolves every preexisting edit path to
its latest accepted direct M16 phase-manifest row, replays the Phase 10
inventory only inside its declared scope, and labels any future unbound
scope-expansion copy `m17_entry_cutoff_only` rather than historical M16 hash
authority. That candidate was superseded by the later schema and lifecycle
repairs.

A second supervisor audit found that "versioned topic-input schema" still left
the disk dispatch under-specified against exact-key M16 validators, and the
master's command example still rendered `--seed` as mandatory. The candidate
now freezes a sibling topic fingerprint V3, GENESIS V2, and contract V3 family,
dispatches from exact persisted schema, rejects cross-family fields, preserves
explicit-seed V2 bytes/vectors, and shows separate topic-only and explicit-paper
forms. The round-2 packet was superseded before final agreement by the compact
round-3 review surface.

The subsequent compact M17 and governance reviews both returned `REVISE` with
eight material findings. Repair round 3 added: a durable at-most-once
capability-call journal; exact topic mission-control/public-result seed
semantics; literal accepted source-manifest hashes; a gated next-subplan review
loop; identified-clean-commit authority for evidence-bearing M19-M23 bytes; an
M20 exhausted-matrix blocker; M21 confirmation lineage/no-call/no-extra-prompt
checks; and M22 replay-versus-new-external case classification with fresh exact
authority. Those repairs passed focused and full local checks before the
round-4 rereviews.

Round-4 bounded rereviews found two directly caused inconsistencies. First,
prepared-but-not-pointer-selected `selected` results could expose effective
seeds and bootstrap authority despite being pre-pointer non-authority. Second,
the generic successor-subplan loop also applied to terminal M23. Repair round 4
now requires effective seeds and authority only for validated
`selected_complete` state, adds the exact prepared-before-pointer catching test,
restricts successor refresh/review/advance to M17-M22, and gives M23 an explicit
completion-or-blocker terminal branch. Finding-specific and full setup audits
passed before the fifth and final bounded rereviews.

The final M17 rereview returned `AGREE` with no material findings. The final
governance rereview returned `REVISE`: M23 verifies a completion predicate that
requires final close/ledger/handoff agreement before those final control
artifacts are written, creating a circular gate. All packet/material hashes
matched. Because this was round 5 for the material setup-governance blocker, no
additional silent repair/review is permitted. The exact blocker and proposed
two-stage `PENDING_FINAL_PREDICATE` repair are recorded in
`docs/plans/literature_survey_north_star_program_setup_review_nonconvergence_blocker_2026-07-13.md`.

The user then authorized exactly one additional governance repair/review round.
M23, the master, and the runbook now specify non-accomplished pending controls,
a noncircular pre-seal predicate, deterministic terminal generation, no-replace
selector publication, post-selection replay, activation-last authority,
canonical fail-closed resolution, crash recovery, and mirror non-authority.
Focused and full setup checks passed. The extra fresh Codex read-only review
matched all five packet hashes and returned `VERDICT: AGREE` with no material
findings. This resolves the setup blocker; it does not execute M17 or authorize
any later boundary.

A final noncircular administrative replay then proved that M17 and M23 retained
their reviewed hashes directly and that reversing only the enumerated
post-review readiness substitutions reconstructed the exact reviewed master and
runbook hashes. The reviewed terminal-control contract therefore did not drift
when the control plane moved from blocked to ready.

## Decision Table

| Decision | Primary criterion | Hard-veto status | Main uncertainty | Next justified action | Not concluded |
| --- | --- | --- | --- | --- | --- |
| Accept setup candidate | Passed | No open setup hard veto | Runtime implementation remains unexecuted | Begin M17 precheck under its reviewed local-only contract | M17 functionality or north-star completion |
| Keep M17 unstarted at setup close | Satisfied | No M17 product edit or output exists | None material | Hand off `M17_READY_NOT_STARTED` | Idea/topic support |
| Keep M18-M23 gated | Satisfied | All future shells marked non-executable | Runtime specifics intentionally unknown | Refresh at predecessor handoff | Git/live/source/human/product authority |

## Forbidden Claims And Actions

- Do not call this setup a completed milestone or accomplished mission.
- Do not infer M17 product functionality from its detailed plan.
- Do not create future result artifacts before execution.
- Do not stage/commit/push, call providers, retrieve sources, obtain/synthesize
  human decisions, change defaults, or release under setup authority.
- Do not let reviewer agreement authorize a human/runtime/model/source/Git/
  scientific/product boundary.

## Exact Handoff

The setup is now `PROGRAM_SETUP_PASSED_M17_READY_NOT_STARTED`. M17 may begin
with its reviewed entry-hash replay, skeptical precheck, immutable pre-edit
snapshot, and local CPU-only allowlist. M18-M23 remain non-executable until
their actual predecessor refresh/review/authority gates.

## What Is Not Concluded

No idea/topic implementation, Git reproducibility, live provider/citation
behavior, source/PDF/full-text behavior, human review, real-mission success,
operator usability, scientific correctness, literature completeness, product
readiness, release readiness, or north-star completion is concluded.
