# Literature Survey North-Star Visible Execution Ledger

Date: `2026-07-13`
Status: `M17_PASSED_M18_PLAN_REVIEW_PENDING`

## Ledger Contract

This append-oriented record tracks visible supervisory state for the M17-M23
north-star program. It is not evidence that a future milestone ran. Codex is
supervisor/executor; Claude or a fresh Codex agent may be a read-only reviewer
only. Every Python/test action is CPU-only with `CUDA_VISIBLE_DEVICES=-1`.

### 2026-07-13 - Program Setup - PRECHECK

Evidence contract:

- Question: Is the converged north-star roadmap sufficiently specified to run
  through visible milestone gates without inventing authority or stopping on an
  expected repair trigger?
- Baseline/comparator: The round-3-agreed north-star master, canonical milestone
  JSON, M16 local close authority, visible-runbook template, and review-gate
  guide.
- Primary criterion: A contract-complete runbook, ledger, handoff, executable
  M17 subplan, non-executable M18-M23 subplans, exact artifact index, and setup
  close record pass local consistency checks and fresh read-only review.
- Veto diagnostics: Missing phase contract; future phase accidentally
  executable; false M16 hash mismatch; proxy promoted to completion evidence;
  absent approval boundary; detached execution; or review authority overreach.
- Nonclaims: No M17 product edit, Git integration, live provider/source action,
  genuine human review, clean-install acceptance, or north-star completion.

Actions:

- Read the visible-gated execution template and Claude review-gate guide.
- Read the converged master, milestone JSON, M16 close record, and relevant M16
  subplan/runbook patterns.
- Inspected the current CLI/mission-state mismatch and affected M17 surfaces.
- Read the Claude read-only review probe skill because the setup is a large
  bounded review surface and prior environment policy rejected repository
  export.

Artifacts:

- `docs/plans/literature_survey_north_star_gap_closure_master_program_2026-07-13.md`
- `docs/plans/literature_survey_automation_milestones.json`
- `docs/plans/literature_survey_m16_phase10_local_close_record_2026-07-13.md`
- `docs/reviews/literature_survey_north_star_gap_closure_plan_review_verdict_round3_2026-07-13.md`

Gate status: `IN_PROGRESS`

Next action: write and locally audit the complete program setup surface.

### 2026-07-13 - Program Setup - SKEPTICAL_AUDIT

Findings checked:

- Baseline: M16 dirty-tree local evidence is correct; it is not Git or live
  evidence.
- Proxy discipline: file existence, JSON parsing, fixture passes, reviewer
  agreement, counts, and timing cannot close the north-star mission.
- Stop logic: expected empty/unavailable/ambiguous candidate outcomes continue
  to declared repair or handoff paths; only continuation vetoes stop the run.
- Comparison fairness: no stochastic or provider ranking occurs in setup or
  M17; future shells require predeclared comparators and uncertainty roles.
- Hidden assumptions: Git, network, source rights, genuine human identity, and
  release remain separate authority boundaries.
- Environment: M17 is local CPU-only; later environment details cannot be
  frozen before predecessor evidence exists.
- Artifact fitness: future result files must remain absent; future subplans are
  explicit non-executable shells until refreshed.
- Frozen hashes: `5280...` labels the Phase 10 result, while the close record's
  own current hash is `5b2d...`; the four authority hashes in the close record
  are the required entry predicate.

Gate status: `PASSED_FOR_PROGRAM_SETUP_DRAFTING`

Next action: create the setup artifacts, then run exact local checks and fresh
read-only review.

### 2026-07-13 - Program Setup - REPAIR_ROUND_1

Evidence contract:

- Question: Did the first M17 snapshot wording incorrectly treat the Phase 10
  validation-tree inventory as product-file hash authority?
- Baseline/comparator: The Phase 10 inventory's declared scope and accepted M16
  Phase 1-9 change manifests.
- Primary criterion: Each preexisting M17 edit path resolves to its latest
  accepted direct M16 hash, while the Phase 10 `1,137`-row inventory is replayed
  only inside its actual validation-tree scope.
- Veto diagnostics: Repository planning files treated as inventory extras;
  passed tests substituted for missing product hashes; entry-cutoff bytes
  mislabelled historical M16 authority; or an unbound path edited silently.
- Nonclaims: This repair does not create the snapshot or execute M17.

Actions:

- Withdrew the round-1 review candidate before agreement.
- Confirmed the Phase 10 inventory enumerates only `positive/**`, `negative/**`,
  and `e2e_static_audit.json` under its validation root.
- Confirmed every preexisting initial M17 edit candidate except an unnecessary
  `survey/__init__.py` path has accepted direct M16 phase-manifest hashes.
- Removed `survey/__init__.py` from the initial allowlist.
- Required per-path latest-authority resolution and an explicit
  `m17_entry_cutoff_only` classification for any future unbound scope expansion.

Gate status: `REPAIR_APPLIED_CHECKS_AND_ROUND_2_REVIEW_PENDING`

Next action: rerun structural checks and freeze a repaired round-2 packet.

### 2026-07-13 - Program Setup - REPAIR_ROUND_2

Evidence contract:

- Question: Did the setup leave topic-schema identity dispatch or the target
  command surface ambiguous enough to risk changing explicit-seed V2 bytes?
- Baseline/comparator: Existing exact-key GENESIS V1, fingerprint V2, and
  contract V2 validators plus the canonical optional-seed target surface.
- Primary criterion: Freeze a sibling topic schema family and on-disk dispatch
  rule while showing both topic-only and explicit-paper command forms.
- Veto diagnostics: Optional keys added to V2; empty-list heuristic selects a
  schema; selected candidates enter original identity; legacy migration is
  reclassified topic-only; or the master still renders a seed as mandatory for
  the target mission.
- Nonclaims: No schema or CLI implementation has run.

Actions:

- Added exact topic fingerprint V3, GENESIS V2, and contract V3 keys and
  cross-family rejection rules.
- Added topic mission-control V3, contract-family pairing, and actual
  mission-control artifact-row schema binding; generic container V1 schemas may
  remain only under tests proving their exact structures do not change.
- Required persisted-schema dispatch, fixed vectors for both families, and a
  schema-aware common input view that never promotes derived effective seeds to
  original inputs.
- Specified CLI omission versus explicit-empty/programmatic-empty behavior.
- Changed the master example to show separate topic-only and explicit-paper
  forms while stating the current code implements only the latter.

Gate status: `REPAIR_APPLIED_CHECKS_AND_FRESH_REVIEW_PENDING`

Next action: rerun focused/full planning checks and submit a compact fresh
M17 review packet.

### 2026-07-13 - Program Setup - REVIEW_ROUND_3_REVISE

The broad review path stalled and was replaced, per the bounded review-probe
procedure, by separate compact M17 and governance reviews. Both fresh Codex
read-only fallback reviews returned `VERDICT: REVISE`.

M17 findings:

1. No durable capability-call lifecycle covered crash after possible invocation
   and before a complete attempt.
2. Topic mission-control/public-result seed fields were not exact enough to
   prevent derived candidates appearing as original inputs.
3. Phase 1-9 source-manifest hashes were recorded only at execution time, not
   frozen as accepted plan inputs.

Governance findings:

1. The runbook advanced unconditionally after next-subplan review.
2. M20-M22 could generate promotion evidence from locally changed,
   non-committed execution bytes.
3. M20 lacked a non-passing terminal when its frozen matrix exhausted without a
   selected candidate.
4. M21 lacked explicit confirmation-lineage/no-call/no-extra-prompt gates.
5. M22 did not classify replay-only versus new external actions or require
   fresh exact authority for the latter.

Gate status: `REVISE_REPAIR_ROUND_3_IN_PROGRESS`

### 2026-07-13 - Program Setup - REPAIR_ROUND_3

Actions:

- Added an at-most-once capability journal with durable `intent`,
  `call_started`, `result_recorded`, `prepared`, and `selected` transitions;
  an indeterminate post-call crash blocks ordinary retry.
- Froze exact topic mission-control V3 and public orchestration-result V3 keys,
  with original seeds empty and derived effective seeds separately bound.
- Froze literal SHA-256 values for all nine accepted M16 source manifests.
- Added a five-round next-subplan review/repair gate and blocked unconditional
  advance.
- Added a program execution-byte authority gate requiring an identified clean
  commit before evidence-bearing M19-M23 actions.
- Bound M20-M22 shells to that gate; added M20 exhausted-matrix blocker, M21
  confirmation gates, and M22 per-case replay/external classification plus
  fresh authority.

Gate status: `REPAIR_APPLIED_FOCUSED_CHECKS_AND_REREVIEW_PENDING`

Next action: run finding-specific and full setup checks, then request focused
rereviews.

### 2026-07-13 - Program Setup - REVIEW_ROUND_4_REVISE

Both bounded fresh Codex read-only rereviews returned `VERDICT: REVISE` after
the round-3 repairs.

M17 finding:

- A prepared `selected` outcome could populate public effective seeds and
  `bootstrap_authority` before current-pointer selection, contradicting the
  pre-pointer non-authority rule.

Governance finding:

- The generic successor-subplan loop also applied to terminal M23, contradicting
  M23's no-automatic-next-milestone contract.

The reviewers independently verified the packet hashes. They found the other
round-3 repairs materially closed.

Gate status: `REVISE_REPAIR_ROUND_4_IN_PROGRESS`

### 2026-07-13 - Program Setup - REPAIR_ROUND_4

Actions:

- Required mission-control/public effective seeds and bootstrap authority to
  remain empty/null until `selected_complete` and a validated current pointer
  plus selected/reconciled journal row bind the exact prepared set.
- Required prepared-before-pointer crash/resume tests to prove null authority,
  empty effective seeds, zero downstream calls, and zero new capability calls
  before exact selection/reconciliation.
- Restricted successor-subplan refresh/review and `ADVANCE()` to M17-M22.
- Added an explicit M23 terminal branch that verifies the frozen completion
  predicate, writes exact accomplished or blocker close/ledger/handoff state,
  and terminates without inventing a successor milestone.
- Repaired matching generic wording in the master program.

Checks:

- Finding-specific audit: `PASS`; all eight round-3 repairs and both round-4
  repairs are explicit.
- Full setup audit: `PASS`; 12 setup artifacts, seven subplans, exact active and
  non-executable states, seven absent future results, four frozen M16 roots,
  nine accepted source-manifest hashes, required sections, milestone JSON,
  indexes/fences, `git diff --check`, and whitespace all passed.
- The JSON parse ran CPU-only with `CUDA_VISIBLE_DEVICES=-1`.

Gate status: `REPAIR_APPLIED_FINAL_ROUND_5_REREVIEWS_PENDING`

Next action: run the fifth and final bounded rereviews for these material
blockers. A material `REVISE` at round 5 requires a nonconvergence blocker and
human direction; it must not be silently extended.

### 2026-07-13 - Program Setup - REVIEW_ROUND_5_NONCONVERGENCE_STOP

Verdicts:

- M17: `VERDICT: AGREE`; no material findings; packet and M17 hashes matched.
- Governance: `VERDICT: REVISE`; packet, runbook, master, and M23 hashes matched.

Material governance blocker:

- The M23 branch verifies its full completion predicate before writing final
  close/ledger/handoff controls, while that predicate requires those controls to
  agree. The gate is circular.

Decision:

- The five-round cap for this material setup-governance blocker fired.
- Wrote
  `docs/plans/literature_survey_north_star_program_setup_review_nonconvergence_blocker_2026-07-13.md`.
- Did not patch beyond the cap, start M17, create a milestone result, mutate Git,
  or cross any live/source/human/product boundary.

Gate status: `PROGRAM_SETUP_BLOCKED_REVIEW_NONCONVERGENCE_M17_NOT_STARTED`

Exact next action: obtain explicit human approval for one additional narrowly
scoped two-stage M23 final-control repair and one bounded review, or retain this
stop.

### 2026-07-13 - Program Setup - EXTRA_GOVERNANCE_REPAIR_AUTHORIZED

Authority:

- The user approved exactly one additional narrowly scoped setup-governance
  repair and review round, limited to the fail-closed two-stage M23
  `PENDING_FINAL_PREDICATE` control transaction, atomic final selection, and
  post-selection consistency replay.

Skeptical audit:

- Several control files cannot be atomically rewritten as a unit. The repair
  therefore uses immutable generations plus one atomic manifest selector.
- A selector alone remains non-authoritative because a crash can occur before
  its disk replay. A matching last-written activation receipt is required.
- The pre-seal predicate cannot require future selector/receipt artifacts; it
  checks substantive criteria and non-accomplished candidate-control agreement.
- Loose controls, mirrors, file existence, labels, and proxy counts remain
  non-promotion evidence.

Repair applied:

- Added exact pending/pre-seal/terminal/selector/replay/activation stages and
  schema keys to M23.
- Mirrored the two-stage noncircular authority rule in the master and runbook.
- Required exact crash/recovery, corrupt/stale/foreign, missing-receipt, and
  canonical-resolver tests.

Gate status: `AUTHORIZED_EXTRA_REPAIR_APPLIED_CHECKS_AND_ONE_REVIEW_PENDING`

No M17 product work, future milestone execution, Git mutation, provider,
network, source, human-decision, GPU, default, scientific-claim, or release
action was performed.

### 2026-07-13 - Program Setup - EXTRA_GOVERNANCE_REVIEW_AGREE

Review:

- Packet:
  `docs/reviews/literature_survey_north_star_program_setup_governance_extra_authorized_review_packet_2026-07-13.md`.
- Verdict:
  `docs/reviews/literature_survey_north_star_program_setup_governance_extra_authorized_review_verdict_2026-07-13.md`.
- Fresh Codex read-only fallback returned `VERDICT: AGREE` with no material
  findings and independently matched all five packet hashes.
- Claude export remained policy-rejected and was not retried or routed around.

Decision:

- The circular M23 setup-governance blocker is resolved.
- Program setup passes as `PROGRAM_SETUP_PASSED_M17_READY_NOT_STARTED`.
- M17 remains unexecuted; M18-M23 remain non-executable shells.
- Reviewer agreement authorizes no human, Git, network, source, product,
  scientific, default, or release boundary.

Gate status: `PROGRAM_SETUP_PASSED_M17_READY_NOT_STARTED`

Exact next action: run M17 precheck under its dedicated reviewed local-only
subplan.

### 2026-07-13 - Program Setup - FINAL_NONCIRCULAR_ADMIN_REPLAY

- M17 directly retained reviewed SHA-256
  `0e177305986803f73543a7687fbd54cf34cca3b4f44898a7550d22858c27cd31`.
- M23 directly retained reviewed SHA-256
  `cbca6e2590d791172ef9fe7b9114a3a7e6f932f337c3646ba37a4142854f1d76`.
- Reversing only the enumerated post-`AGREE` readiness status/table/evidence
  substitutions in a read-only stream reconstructed reviewed master SHA-256
  `a6c3e454f148d8ffdb0c5e532a7ec36a582cb084f62b78b7cc0e6457f5c21a5e`.
- The equivalent read-only reversal reconstructed reviewed runbook SHA-256
  `587c702c84b352b4dd8446fb93485d102396116e36917f9035f0a5dec070cee3`.
- Therefore the accepted terminal-control contract did not drift after review;
  only administrative setup readiness and evidence links changed.

Gate status: `FINAL_NONCIRCULAR_ADMIN_REPLAY_PASSED`

### 2026-07-13 - M17 - ENTRY_PRECHECK_AND_SNAPSHOT_PASSED

Skeptical audit:

- The accepted baseline is the frozen M16 explicit-seed local implementation,
  not Git `HEAD`, a clean checkout, a live provider, or fixture relevance.
- Bootstrap fixture outcomes are contract evidence only. Candidate counts,
  ordering, scores, labels, latency, and artifact size are explanatory and
  cannot establish live retrieval quality, importance, completeness, or
  scientific validity.
- The topic must remain mission input rather than a fabricated paper seed;
  selected candidates must remain separately hash-bound effective authority and
  must never change the mission ID, fingerprint, GENESIS, or original seed list.
- Durable confirmation must be an ancestor of `call_started`; an indeterminate
  call cannot be retried on ordinary resume. Prepared-before-pointer evidence is
  non-authoritative and cannot expose effective seeds or enable downstream work.
- Empty, ambiguous, unavailable, capped, corrupt, stale, foreign, and crash
  outcomes are required fail-closed cases, not reasons to weaken predicates.
- Existing explicit-seed V2 canonical bytes and behavior are the promotion
  comparator. Adding optional fields to V2 or using proxy test counts as a
  compatibility claim is forbidden.
- The local deterministic harness, immutable entry snapshot, lineage replay,
  and cumulative successor manifest answer the M17 engineering question. They
  do not answer live, Git-integration, source, human-review, product, or mission
  completion questions.
- No required M17 command needs network, provider, source/PDF/full-text,
  credentials, Git mutation, model-worker execution, GPU, package installation,
  or writes outside the reviewed allowlist.

Precheck evidence:

- Four of four frozen Phase 10 authority roots matched.
- All `38` direct Phase 10 manifest rows replayed exactly.
- The scoped Phase 10 inventory re-enumerated exactly as `1,137` rows (`1,136`
  regular files and `1` symlink) with tree SHA-256
  `b6d5ddb4f52238abadaa07b5bd80ed74e478dab64088efa65eac3c7fd6c09d41`.
- All nine frozen Phase 1-9 source-manifest hashes matched.
- All ten preexisting initial edit candidates resolved unambiguously to their
  latest accepted direct M16 row, and current bytes matched every selection.
- The M17 validation and fixture roots were absent at entry; no unexplained
  overlapping dirty work was found.

Immutable snapshot:

- Root:
  `docs/validation/literature_survey_m17_2026-07-13/pre_edit_m16_snapshot/`.
- `entry_replay.json` SHA-256:
  `6c4890cb9bbbc3ce12f925b5262626d73bcfced53302703196df4baf5eb13b15`.
- `authority_resolution.json` SHA-256:
  `f5dada6809ce604a50b63efc1eb24a8999b409f51c35f94341af4c36428525ae`.
- `snapshot_manifest.json` SHA-256:
  `752b3d72c50d7e3eb45ba47a19528308e0c69faa5692bb49a6dedf85c1d5c340`.
- `snapshot_check.json` SHA-256:
  `5101b6959700b6fdc5bc3ee984ea12f8968413bf9d8679497bcc40ea6f5d099a`.
- A cross-device `/tmp` rename attempt failed before installing any repository
  path. The verified bundle was then copied through same-filesystem staging,
  replayed again, atomically selected as the snapshot subtree, and sealed
  read-only. Product/test bytes remained unchanged throughout.

Decision: the M17 entry continuation veto did not fire. Implementation may
start under the exact reviewed product/test allowlist with
`CUDA_VISIBLE_DEVICES=-1` for every Python/test command.

Gate status: `M17_ENTRY_PASSED_SNAPSHOT_FROZEN_IMPLEMENTATION_ACTIVE`

### 2026-07-14 - M17 - LOCAL_ENGINEERING_AND_CLOSURE_PASSED

- Topic-only startup is implemented without fabricated original seeds;
  explicit-seed V2 behavior is unchanged.
- Authoritative gates passed: `65` focused M17 including 38 crash points,
  `846` cumulative M16+M17, `13/13` persistent cases, `11` exact scripts,
  `20` affected integration, `1,050` full unit, and `125` full CLI; six JUnit
  files total `2,117` passes with zero failure/error/skip.
- The first 502-row successor manifest under-covered Phase 10. M17 was reopened,
  repaired to `1,671` unique sorted paths, and replayed with all `1,137`
  inventory members and all `38` direct logical rows.
- Claude Opus/max code review returned `AGREE`. A second Claude export for the
  closure repair was policy-rejected; a fresh Codex read-only fallback returned
  `AGREE` and the rejection was not routed around.
- Result:
  `docs/plans/literature_survey_north_star_m17_idea_topic_bootstrap_result_2026-07-13.md`.

Gate status: `PASSED_LOCAL_ENGINEERING_GIT_INTEGRATION_PENDING`

### 2026-07-14 - M18 - SKEPTICAL_AUDIT_AND_PREFLIGHT

- Current policy migration retires special approval-token ceremony. The user's
  execute/resume request authorizes reviewed non-destructive local integration;
  push, release, destructive, live/source, and human boundaries remain gated.
- Exact M17 payload audit found three missing SurveyBench prompt inputs and the
  10-file Phase 6 packet needed by the exact script suite. All 13 canonical
  inputs were added explicitly.
- The inherited M17 CLI contains four optional arXiv plan-file lines whose
  implementation exists only in excluded unrelated dirty work. The committed
  candidate removes exactly those four lines without changing worktree bytes.
- Disposable baseline clone preflight passed: compile/import/arXiv decoupling,
  `65` M17 tests, `22` SurveyBench tests, `8` CLI tests, and after dependency
  repair all `11` exact script tests.
- Payload: `1,684` unique sorted paths, SHA-256
  `c378d271ef8402e7980437b4e46ee25557872d30df0725cdace0fc25e0afd172`.
- Six unrelated tracked worktree changes remain protected and the index remains
  empty. No commit, package install, network, provider, source, GPU, credential,
  or release action has occurred.

Gate status: `M18_EXECUTION_READY_PENDING_MATERIAL_PLAN_REVIEW`
