# M20B3 Identified Integration And Exact Live Packet Subplan

Date: `2026-07-15`
Status: `PASSED_IDENTIFIED_INSTALL_AND_PACKET_REVIEW_M20B4_EXACT_HUMAN_AUTHORIZATION_REQUIRED`
Milestone: `M20_live_discovery_and_citation_frontier`

## Phase Objective

Integrate only the exact M20A/M20B2-passed payload into an identified Git
commit and isolated wheel, implement the bounded M20 worker/supervisor around
the reviewed credential/cost boundary without using a real key or network,
prove installed-member equality and synthetic-canary behavior on those exact
bytes, then freeze and materially review one exact M20B4 live packet.

This phase does not inspect or use a real credential, call a provider, spend
usage cost, execute the packet, access sources, start M21, push, or release.

## Entry Conditions Inherited From M20B2

- M20B1 official contract evidence and its consumed attempt replay exactly.
- The approved future interface remains only `OPENALEX_API_KEY`; the privacy
  rules, two-class synthetic occurrence contract, and `$0.01` cap remain
  unchanged.
- M20B2 authoritative code/test/evidence hashes are terminally reviewed with
  no unresolved material finding.
- The human explicitly authorizes the exact bounded M20B3 Git payload,
  stage/commit, isolated-clone, and offline-wheel integration operation. No
  M20B2 review verdict, prior execute/resume statement, or local test pass may
  substitute for that new authority. Current authority explicitly excludes
  Git integration.
- The credential-free OpenAlex descriptor/parser remains byte-identical.
- No real credential value, provider call, M20B4 authorization, source access,
  push, or release authority exists.

Human authority recorded `2026-07-15`: the user approved the exact bounded
M20B3 Git payload, staging/commit, isolated clone, and offline-wheel integration
operation described by this plan. This approval does not extend to a real key,
provider call, M20B4, source access, push, or release.

## Refreshed Skeptical Pre-Execution Audit

Status: `PASS_AUTHORIZED_LOCAL_INTEGRATION_ONLY`.

- Baseline/parent: Git `HEAD` is the M19 close commit
  `ad4e2d52ab9df7198547b3cb98d8acbd1b9680a5`; M20A/M20B2 passed bytes are
  exact untracked/modified worktree artifacts and must be integrated by an
  explicit path allowlist, never a directory or wildcard stage.
- Protected work: unrelated changes in `.gitignore`, `docs/benchmark_plan.md`,
  `src/research_assistant/cli.py`, arXiv intake/source code, and their tests are
  outside the payload and must remain unstaged.
- Proxy discipline: wheel equality, synthetic canary absence, tests, and packet
  freeze establish local engineering only. They do not establish provider
  behavior, key availability, actual billing, scientific validity, or M20
  completion.
- Environment: local checks intentionally remove `OPENALEX_API_KEY`, set
  `CUDA_VISIBLE_DEVICES=-1`, and use injected fake transports. The future real
  worker may define an exact named-variable getter but M20B3 must not invoke it
  with a real value.
- Transport design: reuse the established M19 bounded arXiv request semantics;
  all three OpenAlex requests must pass through the reviewed M20B2
  credential/cost boundary. The exact order is arXiv topic, OpenAlex topic,
  arXiv exact seed, OpenAlex direct, OpenAlex forward.
- Packaging: implement worker and supervisor as package modules invoked with
  `python -m`; this avoids a public CLI/default change and ensures the wheel is
  the execution payload.
- Artifact fitness: the packet binds a successor commit/tree/wheel, exact
  installed members, five descriptors, caps, absent live root, and command. It
  is a proposal for separate M20B4 authorization, not execution authority.
- Stop logic: a synthetic or infrastructure defect triggers local repair and a
  fresh versioned validation root. A real-key need, provider/network need,
  outside-payload edit, protected overlap, or failed installed equality is a
  continuation veto for M20B3.

## Authorized Git Payload And Protected Inventory

The payload will be frozen before staging in
`docs/validation/literature_survey_m20b3_identified_integration_2026-07-15/payload_manifest.json`.
It may contain only:

- reviewed runtime: `src/research_assistant/survey/discovery_capability.py`,
  `src/research_assistant/survey/openalex_adapter.py`, and
  `src/research_assistant/survey/openalex_credential_cost.py`;
- new M20 package worker/supervisor modules and their exact tests;
- M20A/M20B0-M20B3 plans, results, reviewed manifests, and review verdicts
  needed to reproduce the active authority chain;
- current north-star milestone/master/runbook/ledger/handoff/reset controls;
  and
- M20B3 validation, packet, result, and review artifacts created by this phase.

Historical raw provider/documentation bodies, superseded M20B2 roots, unrelated
program histories, and every protected path named above remain outside the
commit unless an exact later audit proves a specific artifact is required and
the payload manifest is visibly repaired before staging.

## Required Artifacts

- exact Git payload allowlist and protected dirty-path inventory;
- one identified successor commit/tree with single-parent lineage from the
  current M20 authority;
- isolated clone, offline-built wheel, installed module origins, complete wheel
  member inventory, and byte-for-byte installed-member equality;
- bounded M20 worker/supervisor with exact five-request order, route/body/time/
  process/IPC/write caps, no redirects/retries/proxy, accepted-body retention,
  cost reservation/reconciliation, and closed credential/error evidence;
- synthetic-canary success/failure matrix repeated against the exact installed
  worker, with no occurrence outside the reviewed ephemeral source/request
  classes on enumerated surfaces;
- exact absent live root, command, commit/tree/wheel, routes, budgets, stop
  conditions, one-attempt rule, nonclaims, and preflight packet;
- material packet review verdict and an explicit M20B4 human decision request
  containing no secret value or secret-bearing path.

## Required Checks, Tests, And Review

- Audit wrong parent/baseline, mutable installed bytes, weak proxy metrics,
  hidden environment inheritance, cost drift, stale documentation, output-root
  reuse, packet commands that cannot close the five-request evidence contract,
  and any operation outside the exact Git payload.
- Rerun M19 transport/supervisor, M20 discovery/adapter, M20B1, M20B2 focused,
  affected M16/M17, CLI, exact-script, compile, JSON, NUL-safe reconciliation,
  canary/credential scans, and `git diff --check` from the isolated install.
- Verify descriptor/budget/output/authority preflight completes before the one
  exact named-variable lookup in the installed worker.
- Verify the authenticated request and key cannot enter argv, persisted
  descriptors, bodies, ledgers, IPC, streams, logs, exceptions, manifests,
  JUnit, review artifacts, or Git candidates.
- Obtain one material code/integration review and one exact packet review.
  Review is advisory and cannot authorize a real key or provider call. Repair
  material findings for at most five rounds on the same blocker.

## Evidence Contract

| Field | Contract |
| --- | --- |
| Question | Are the exact reviewed M20 discovery plus credential/cost controls reproducible from an identified clean install and sufficient to freeze one bounded live packet without using a key or provider? |
| Baseline | Terminally reviewed M20A and M20B2 bytes plus retained M19/M20B1 evidence. |
| Primary criterion | Exact commit/tree/wheel/member equality, installed synthetic-canary and cost closure, complete five-request worker/supervisor boundaries, absent root, frozen exact packet, and material review pass. |
| Hard vetoes | Wrong/mutable bytes; real-secret access; provider/network call; canary outside authorized ephemeral classes; unknown/contradictory/unreconciled cost; route/cap drift; outside Git payload; reused output; packet/review treated as M20B4 authority. |
| Explanatory only | Build time, wheel size, test duration, report bytes, provider examples, or projected result counts. |
| Not concluded | Real-key availability/use authority, provider behavior, actual balance/billing, live M20 result, M21 readiness, universal leak freedom, product readiness, or scientific validity. |
| Preserving artifacts | Commit/tree/wheel/member manifests, installed origins/equality, tests/JUnit, canary scan, packet, review verdicts, result/blocker, and M20B4 decision request. |

## Forbidden Claims And Actions

- Do not execute before M20B2 terminal review, a refreshed skeptical audit,
  and new explicit human authority for the exact bounded M20B3 Git-integration
  operation.
- Do not inspect/read/use a real key, enumerate the environment, call any
  provider or documentation route, spend usage cost, or create a live result.
- Do not stage/commit unrelated dirty work, rewrite history, push, release, or
  mutate prior evidence roots.
- Do not treat Git integration, installed tests, packet freeze, or reviewer
  agreement as M20B4 execution authority.
- Do not access source/PDF/full-text, start M21, or claim M20/north-star
  completion.

## Exact Next-Phase Handoff Conditions

M20B4 may be requested only after the exact identified install and packet pass
all checks and review, the live root is absent, the authorized key
owner/controller and exact campaign permission are confirmed without exposing
the key, and the human explicitly authorizes exactly one packet-bound live
attempt. No implicit execute/resume statement or M19/M20B1/M20B2 authority may
substitute for that final authorization.

## Stop Conditions

Stop for wrong/unreproducible bytes, protected dirty overlap, canary exposure,
real-secret or network need during M20B3, cost/route uncertainty, failed
affected tests, output-root collision, unreviewed packet drift, or five-round
material nonconvergence. A fixable local integration defect triggers a fresh
versioned artifact and focused repair; it does not authorize M20B4.

## Historical Five-Round Outcome

Status: `BLOCKED_M20B3_REVIEW_NONCONVERGENCE_AFTER_FIVE_ROUNDS_NO_GIT_ACTION`.

Round 5 found that installed-artifact replay still accepts a
producer-impossible `observed_cost_usd` value for blocked-after-dispatch cost
evidence. The five-round stop condition fired before any Git action. The index
remains empty; no isolated clone, wheel, installed validation, packet, real-key
lookup, or provider call occurred.

Phase close record:
`docs/plans/literature_survey_north_star_m20b3_review_nonconvergence_blocker_result_2026-07-15.md`.

Final review record:
`docs/reviews/literature_survey_north_star_m20b3_code_review_verdict_round5_2026-07-15.md`.

Do not resume this subplan unless the human explicitly permits an additional
narrow replay-integrity repair/review round or provides different direction.

## Extra Authorized Replay Repair

On `2026-07-15`, the human approved one additional narrowly scoped local
repair and one terminal review round limited to blocked-after-dispatch
`observed_cost_usd` replay and its tests. This does not reopen Git integration,
M20B4, real credential, provider, source, push, or release authority unless the
terminal review agrees and the original M20B3 gates subsequently pass.

Pre-repair skeptical audit: `PASS_NARROW_REPLAY_REPAIR_ONLY`.

- Baseline: round-5 worker and worker-test hashes were
  `74e28e5099481826008a54a800dde10c314d914d7ecdd6ac7f4714261047b92b`
  and `b3603622905d3b19ae9d7b89e5676f311133515d331f6d955b0e7e4250fa4bd3`;
  the supervisor and its tests remain unchanged.
- The repair question is exact: does replay accept only the producer's
  null/finite/cost-relation shapes for each blocked-after-dispatch error?
- No baseline, route, cost cap, credential interface, privacy surface,
  five-request order, output contract, promotion criterion, or external
  boundary changes.
- The earliest diagnostic is a digest-rebound positive/adversarial matrix for
  every blocked-after-dispatch error, followed by the cumulative M20 suite.
- Installed equality, Git integration, and packet freeze remain downstream and
  cannot be inferred from this local repair.

Implementation checks passed `54` focused worker tests and `227` cumulative
M20 discovery/adapter/credential/worker/supervisor tests with
`OPENALEX_API_KEY` removed, `PYTHONPATH=src`, and
`CUDA_VISIBLE_DEVICES=-1`. Terminal review of the newly frozen bytes is the
active gate.

Terminal review returned `AGREE` on the exact frozen hashes with no material
finding. The five-round blocker is superseded only by this explicitly
human-authorized extra repair/review transaction. The original bounded M20B3
Git/clone/wheel/install/packet-freeze authority was then consumed successfully;
M20B4 and all external boundaries remain forbidden.

## Terminal Phase Outcome

Status: `PASSED_IDENTIFIED_INSTALL_AND_PACKET_REVIEW_M20B4_EXACT_HUMAN_AUTHORIZATION_REQUIRED`.

The exact commit, isolated wheel/install equality, installed synthetic report,
packet preflight, and packet review are recorded in
`docs/plans/literature_survey_north_star_m20b3_identified_integration_and_live_packet_result_2026-07-15.md`.
The next phase is the reviewed M20B4 subplan, which remains non-executable
until exact human authorization for one packet-bound live attempt.
